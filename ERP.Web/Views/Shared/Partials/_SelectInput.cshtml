@model (string Name, string Label, IEnumerable<Microsoft.AspNetCore.Mvc.Rendering.SelectListItem> Options, int? SelectedValue)
@{
    var fieldName = Model.Name;
}
<div class="mb-4">
    <label class="mb-1 block text-sm font-medium text-slate-700" for="@fieldName">@Model.Label</label>
    <select name="@fieldName" id="@fieldName"
            class="w-full rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500">
        @foreach (var option in Model.Options)
        {
            <option value="@option.Value" selected="@(Model.SelectedValue?.ToString() == option.Value)">@option.Text</option>
        }
    </select>
    <span asp-validation-for="@fieldName" class="text-sm text-red-600"></span>
</div>
